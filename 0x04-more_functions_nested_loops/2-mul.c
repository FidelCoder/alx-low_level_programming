/**
 * mul - gets the multiple of 2 numbers
 * @a : first int
 * @b : second int
 *
 * Return: On success int
 * On error, 0 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);
}
