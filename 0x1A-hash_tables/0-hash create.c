#include "hash_tables.h"
/**
 * create_t - create an item 
 * @key : key
 * @value : value
 * Returns: Item
 */
hash_node_t create_t (char *key, char *value)
{
	hash_node_t *item = malloc (sizeof(hash_node_t));
	if (item == NULL)

}